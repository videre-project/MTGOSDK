load("@rules_dotnet//dotnet:defs.bzl", "csharp_library")

package(default_applicable_licenses = ["//:license"])

licenses(["notice"])  # MIT

csharp_library(
    name = "ScubaDiver.API.dll",
    srcs = ["**.cs"],
    private_deps = ["@rules_dotnet_nuget_packages//microsoft.netcore.app.ref"],
    # private_deps = ["@rules_dotnet_nuget_packages//netstandard.library"]
    deps = [
        # "@RemoteNET//Microsoft.Diagnostics.Runtime"
        # "@RemoteNET//Newtonsoft.Json"
    ],
    target_frameworks = ["netstandard2.0"],
    visibility = ["//visibility:public"],
)
